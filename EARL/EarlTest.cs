using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Xml;
using tud.mci.tangram.models;
using tud.mci.tangram.qsdialog.models;


namespace tud.mci.tangram.EARL
{
    class EarlTest
    {
        static int Main(string[] args)
        {
            //XmlDocument xDoc = new XmlDocument();

            //string baseDir = AppDomain.CurrentDomain.BaseDirectory;
            //xDoc.Load(baseDir + @"\document.rdf");
            //Earl c1 = new Earl();
            //EarlReader e1 = new EarlReader();
            //EarlReader earlreader = new EarlReader();
            //Evaluation eval = new Evaluation();
            //List<Category> categories = Parser.parsing();
            //c1.createEmptyEarlGraph(MediaType.all);
            //e1.updateDialog(categories, xDoc);
            //eval.evaluate(categories, new Parameters(), c1);
            //c1.writeEarlRdf();

            //earlreader.readingEARL("earl.rdf");
            //earlreader.createStringLists();
            //Console.ReadLine();

            return 0;
        }
    }

}
